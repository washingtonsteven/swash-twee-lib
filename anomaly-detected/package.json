{
  "name": "anomaly-detected",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "node ../bin/build.js --project anomaly-detected",
    "start": "yarn serve ./build",
	"watch": "nodemon --ext twee --exec \"yarn build\""
  },
  "tweeConfig": {
	"source": "./src/**/*.twee"
  }
}
